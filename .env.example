# --- Server ---
PORT=8080
NODE_ENV=development

# Comma-separated list (no spaces) or * for all
CORS_ORIGIN=http://localhost:5173

# --- Firebase Admin (Service Account) ---
# You can either provide GOOGLE_APPLICATION_CREDENTIALS that points to a JSON file
# OR define the 3 vars below (recommended for containers/CI). If using FIREBASE_PRIVATE_KEY
# paste it with \n newlines escaped, e.g. -----BEGIN PRIVATE KEY-----\nABC...\n-----END PRIVATE KEY-----\n
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_WITH_KEY\n-----END PRIVATE KEY-----\n"
